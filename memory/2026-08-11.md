# Memoria Giornaliera — Martedì 11 Agosto 2026

Questo file documenta l'intero riepilogo delle operazioni e degli sviluppi eseguiti oggi, suddivisi tra l'infrastruttura di OpenClaw e il progetto di trading evolutivo `krakenfondazione` con la sua VPS di inferenza.

---

## 1. Infrastruttura OpenClaw & Gateway (Zava U50)

### Problemi Rilevati & Risolti:
1. **Memory Search (Ricerca Semantica):**
   * Era precedentemente bloccata per assenza di credenziali OpenAI.
   * Modificata la configurazione (`openclaw.json`) per utilizzare il provider **`gemini`** (visto che Giacomo ha un profilo di autenticazione Google valido ed efficace: `google:default`).
   * Creata la cartella del workspace `memory/`, inizializzato il file a lungo termine `MEMORY.md` e rigenerato con successo l'indice vettoriale con **3072 dimensioni** (tramite `openclaw memory index --force`).
   * La ricerca semantica è ora pienamente operativa.

2. **Gateway Bind & Connettività Loopback (`gateway.bind`):**
   * Il Gateway era associato unicamente all'interfaccia Tailscale (`tailnet`), bloccando ogni connessione locale da loopback (`127.0.0.1:18789`) effettuata dai tool interni (es. `cron` o lo stesso `memory_search` della sessione).
   * Modificato `gateway.bind` in **`auto`** affinché ascolti sia localmente sia sulla rete Tailscale.
   * Riavviato con successo il servizio Gateway tramite systemd (`openclaw gateway restart`).

---

## 2. ClawForge (Sub-Agente 1) & Capacità UI/Dashboard

### Sviluppi & Integrazioni:
* **Installazione di `dynamic-ui`:** Installata con successo la skill `theashbhat-dynamic-ui@1.2.0` da ClawHub nel workspace.
* **Risoluzione Dipendenza Obsoleta (`wkhtmltoimage`):**
  * Poiché `wkhtmltoimage` non è più supportato/disponibile sui repository di Ubuntu moderni (su Zava U50), lo script `render.sh` della skill è stato modificato per utilizzare **Headless Chromium** (tramite snap).
  * Risolto il blocco di sicurezza di snap (che non scrive in cartelle nascoste come `.openclaw`) facendo scrivere temporaneamente l'immagine in `/home/tre/rendered.png` per poi spostarla nel workspace finale.
* **Test di Rendering:** Eseguito con successo il rendering di una tabella (`test_table.png`) e di un grafico a barre (`test_chart.png`).
* **Dashboard Control Center:**
  * Creata un'interfaccia HTML interattiva a `~/.openclaw/canvas/index.html` (chiamata **ClawForge Control Center**) con Tailwind CSS, Chart.js e Lucide.
  * Risolto l'errore di autorizzazione `401 Unauthorized` degli iframe integrati in chat (che non trasmettevano il token del Gateway) catturando uno screenshot headless della dashboard (`dashboard.png`).

---

## 3. Progetto Kraken Fondazione (`krakenfondazione`)

### Stato Attuale:
* **5 Strategie in Esecuzione (Paper Trading):**
  * Orizzonti temporali: 24h, 48h, 72h, 7d, 30d su coppia **BTC/USD**.
  * Capitale di partenza: **$343.41** per ciascuna corsa.
  * Floor protettivo rigido impostato al **60% dello start equity** (es. ~$206). Target di rendimento: **$5,500.00**.
  * Al momento le strategie sono piatte (0 trades, 0% PnL, $0.00 margine bloccato) in stato di **HOLD** forzato.

### Problemi Tecnici Rilevati:
1. **Inference AI (Stallo del Giudice):** Il modello Nemotron-3-Nano remoto fallisce nel restituire l'output strutturato JSON (risposte vuote o formattate male). Al terzo tentativo fallito, il sistema attiva il failsafe registrando *"L'orco ha fallito il parsing del JSON strutturato"* ed esegue un `HOLD` cautelativo.
2. **Worker Interrotto:** Il worker di polling in background (`app.cli`) che interroga il mercato risulta attualmente spento o crashato (`STOPPED`).

### Soluzione Bug & Isolamento Test:
* **Risoluzione Bug ZEUR:** Corretto l'allineamento dei decimali e la conversione EUR/USD per ZEUR in `app/core/snapshot.py`, permettendo il calcolo corretto dell'equity iniziale in dollari sul paper ledger.
* **Isolamento DB dei Test:** Risolto il leak per cui i test d'integrazione scrivevano sul database operativo di produzione `krakenfondazione.db` a causa del ripristino errato di `DATABASE_URL` nel `tearDownClass`.

### Aggiornamento UI Dashboard di Trading:
* Riscritto l'intero front-end Streamlit (`app/dashboard/app.py`, porta `8501`) con un design scuro e professionale in italiano. Mostra:
  * Top Risk/Status Bar globale con equity aggregata, capitale totale, margine bloccato e stato corse.
  * Grafico combinato Plotly per la curva delle equity e leaderboard PnL%.
  * 5 Strategy Cards individuali con scadenze temporali e floor calcolati.
  * Deep Inspection per singolo run con tab dedicate a Chart/Timeline, Nemotron Roles (inclusi *Skeptic Analyst* e *Judge*), allocazione asset, transazioni (Fills) e log SQLite raw.
* Le modifiche sono state committate con successo su git: `style(dashboard): professional trading-desk UI per esperimento 5 run`.

---

## 4. VPS di Inferenza Nemotron (`instance-20260719-152821`)

* **Infrastruttura:** Debian 13 (trixie), 8 vCPU Intel Xeon, ~50 GB RAM, **2 GPU NVIDIA Tesla T4** (15GB VRAM ciascuna) con driver 550.163.01 e CUDA 12.4.
* **Stack di Inferenza:** Server `llama.cpp` che serve il modello **NVIDIA Nemotron 3 Nano 30B-A3B** (UD-Q4_K_XL, ~22.8 GB, distribuito da Unsloth), configurato come servizio systemd `kraken-nemotron.service`.
* **API Privata & Connettività:**
  * Esibito unicamente sulla rete interna Tailscale all'indirizzo `http://100.73.54.72:8080/v1` (OpenAI-compatible).
  * Nessuna esposizione pubblica, nessun software di trading o credenziale Kraken caricata per massimizzare la sicurezza.
* **Prestazioni basatrici:** Generazione a circa **~52.5 tokens/s** nei test di completamento JSON.
* **Contratto di Handoff per TRE:**
  ```text
  AI_PROVIDER=openai_compatible
  AI_BASE_URL=http://100.73.54.72:8080/v1
  AI_MODEL=<rilevato dinamicamente da /v1/models>
  AI_API_KEY=
  ```
